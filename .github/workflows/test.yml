name: test

on:
  - push
  - pull_request

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: install
        run: make install

      - name: lint
        run: make lint

      - name: test
        run: make test